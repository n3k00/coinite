import 'package:flutter/material.dart';

class FearIndexPage extends StatelessWidget {
  const FearIndexPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Fear Index"),
    );
  }
}
